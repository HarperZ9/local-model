<?xml version="1.0" encoding="utf-8"?><testsuites name="pytest tests"><testsuite name="pytest" errors="0" failures="6" skipped="0" tests="9" time="0.225" timestamp="2026-07-14T09:35:31.332792-07:00" hostname="DESKTOP-3HC9JE1"><testcase classname="artifacts.uplift.work_20260714-091030.days_between.tests.test_solution" name="test_same" time="0.001"><failure message="NameError: name 'dpm' is not defined">&gt;   def test_same(): assert f((1970, 1, 1), (1970, 1, 1)) == 0
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests\test_solution.py:3: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

d1 = 1, d2 = 1

    def days_between(d1, d2):
        if not (isinstance(d1, tuple) and isinstance(d2, tuple)):
            raise ValueError('bad date')
        def is_int(v):
            return isinstance(v, int) and type(v) is int
        if not all(is_int(a) for a in [d1[0], d1[1], d1[2]]):
            raise ValueError('bad date')
        if not all(is_int(a) for a in [d2[0], d2[1], d2[2]]):
            raise ValueError('bad date')
        y1, m1, d1 = d1
        y2, m2, d2 = d2
        # 365 days per year; leap-day correction (extra day in February of each
        # divisible-by-four year except centuries unless also a four-hundred-year
        # multiple) is handled at the level of total DAYS rather than months or years,
        # so there is no need to make any month-length table dependent on whether a
        # midpoint year across two calls was a leap year.
        def days_through(y):
            y -= 1
            return 365*y + (y//4) - (y//100) + (y//400)
        def days_in_month(y, m):
            if not (1 &lt;= m &lt;= 12): raise ValueError('bad date')
            # flat table: index is zero-based month 0..11; leap-dependent bit only in
            # the February slot so it is read ONCE at the ONE comparison where a day
            # count depends on it, never during the per-day loop below.
            dpm = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)
            if m == 2:
                extra = 1 if ((y%4 == 0) and not (y%100 == 0)) or (y%400 == 0) else 0
                return dpm[m-1] + extra
            return dpm[m-1]
        # reject a day outside its own month (leap-year February is handled above)
        if not (1 &lt;= d1 &lt;= days_in_month(y1, m1)):
            raise ValueError('bad date')
        if not (1 &lt;= d2 &lt;= days_in_month(y2, m2)):
            raise ValueError('bad date')
        # all input valid; count through the first year to that day's start then each
        # full intervening year, then the last year. Every summand is a known integer,
        # so the whole result is int -- and it may be negative (for d1 later than d2).
&gt;       t = days_through(y1) + sum(dpm[0:m1-1]) + d1 - 1
                                   ^^^
E       NameError: name 'dpm' is not defined

solution.py:38: NameError</failure></testcase><testcase classname="artifacts.uplift.work_20260714-091030.days_between.tests.test_solution" name="test_next" time="0.000"><failure message="NameError: name 'dpm' is not defined">&gt;   def test_next(): assert f((1970, 1, 1), (1970, 1, 2)) == 1
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests\test_solution.py:4: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

d1 = 1, d2 = 2

    def days_between(d1, d2):
        if not (isinstance(d1, tuple) and isinstance(d2, tuple)):
            raise ValueError('bad date')
        def is_int(v):
            return isinstance(v, int) and type(v) is int
        if not all(is_int(a) for a in [d1[0], d1[1], d1[2]]):
            raise ValueError('bad date')
        if not all(is_int(a) for a in [d2[0], d2[1], d2[2]]):
            raise ValueError('bad date')
        y1, m1, d1 = d1
        y2, m2, d2 = d2
        # 365 days per year; leap-day correction (extra day in February of each
        # divisible-by-four year except centuries unless also a four-hundred-year
        # multiple) is handled at the level of total DAYS rather than months or years,
        # so there is no need to make any month-length table dependent on whether a
        # midpoint year across two calls was a leap year.
        def days_through(y):
            y -= 1
            return 365*y + (y//4) - (y//100) + (y//400)
        def days_in_month(y, m):
            if not (1 &lt;= m &lt;= 12): raise ValueError('bad date')
            # flat table: index is zero-based month 0..11; leap-dependent bit only in
            # the February slot so it is read ONCE at the ONE comparison where a day
            # count depends on it, never during the per-day loop below.
            dpm = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)
            if m == 2:
                extra = 1 if ((y%4 == 0) and not (y%100 == 0)) or (y%400 == 0) else 0
                return dpm[m-1] + extra
            return dpm[m-1]
        # reject a day outside its own month (leap-year February is handled above)
        if not (1 &lt;= d1 &lt;= days_in_month(y1, m1)):
            raise ValueError('bad date')
        if not (1 &lt;= d2 &lt;= days_in_month(y2, m2)):
            raise ValueError('bad date')
        # all input valid; count through the first year to that day's start then each
        # full intervening year, then the last year. Every summand is a known integer,
        # so the whole result is int -- and it may be negative (for d1 later than d2).
&gt;       t = days_through(y1) + sum(dpm[0:m1-1]) + d1 - 1
                                   ^^^
E       NameError: name 'dpm' is not defined

solution.py:38: NameError</failure></testcase><testcase classname="artifacts.uplift.work_20260714-091030.days_between.tests.test_solution" name="test_prev" time="0.000"><failure message="NameError: name 'dpm' is not defined">&gt;   def test_prev(): assert f((1970, 1, 2), (1970, 1, 1)) == -1
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests\test_solution.py:5: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

d1 = 2, d2 = 1

    def days_between(d1, d2):
        if not (isinstance(d1, tuple) and isinstance(d2, tuple)):
            raise ValueError('bad date')
        def is_int(v):
            return isinstance(v, int) and type(v) is int
        if not all(is_int(a) for a in [d1[0], d1[1], d1[2]]):
            raise ValueError('bad date')
        if not all(is_int(a) for a in [d2[0], d2[1], d2[2]]):
            raise ValueError('bad date')
        y1, m1, d1 = d1
        y2, m2, d2 = d2
        # 365 days per year; leap-day correction (extra day in February of each
        # divisible-by-four year except centuries unless also a four-hundred-year
        # multiple) is handled at the level of total DAYS rather than months or years,
        # so there is no need to make any month-length table dependent on whether a
        # midpoint year across two calls was a leap year.
        def days_through(y):
            y -= 1
            return 365*y + (y//4) - (y//100) + (y//400)
        def days_in_month(y, m):
            if not (1 &lt;= m &lt;= 12): raise ValueError('bad date')
            # flat table: index is zero-based month 0..11; leap-dependent bit only in
            # the February slot so it is read ONCE at the ONE comparison where a day
            # count depends on it, never during the per-day loop below.
            dpm = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)
            if m == 2:
                extra = 1 if ((y%4 == 0) and not (y%100 == 0)) or (y%400 == 0) else 0
                return dpm[m-1] + extra
            return dpm[m-1]
        # reject a day outside its own month (leap-year February is handled above)
        if not (1 &lt;= d1 &lt;= days_in_month(y1, m1)):
            raise ValueError('bad date')
        if not (1 &lt;= d2 &lt;= days_in_month(y2, m2)):
            raise ValueError('bad date')
        # all input valid; count through the first year to that day's start then each
        # full intervening year, then the last year. Every summand is a known integer,
        # so the whole result is int -- and it may be negative (for d1 later than d2).
&gt;       t = days_through(y1) + sum(dpm[0:m1-1]) + d1 - 1
                                   ^^^
E       NameError: name 'dpm' is not defined

solution.py:38: NameError</failure></testcase><testcase classname="artifacts.uplift.work_20260714-091030.days_between.tests.test_solution" name="test_leap_cross" time="0.000"><failure message="NameError: name 'dpm' is not defined">&gt;   def test_leap_cross(): assert f((2020, 2, 28), (2020, 3, 1)) == 2
                                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests\test_solution.py:6: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

d1 = 28, d2 = 1

    def days_between(d1, d2):
        if not (isinstance(d1, tuple) and isinstance(d2, tuple)):
            raise ValueError('bad date')
        def is_int(v):
            return isinstance(v, int) and type(v) is int
        if not all(is_int(a) for a in [d1[0], d1[1], d1[2]]):
            raise ValueError('bad date')
        if not all(is_int(a) for a in [d2[0], d2[1], d2[2]]):
            raise ValueError('bad date')
        y1, m1, d1 = d1
        y2, m2, d2 = d2
        # 365 days per year; leap-day correction (extra day in February of each
        # divisible-by-four year except centuries unless also a four-hundred-year
        # multiple) is handled at the level of total DAYS rather than months or years,
        # so there is no need to make any month-length table dependent on whether a
        # midpoint year across two calls was a leap year.
        def days_through(y):
            y -= 1
            return 365*y + (y//4) - (y//100) + (y//400)
        def days_in_month(y, m):
            if not (1 &lt;= m &lt;= 12): raise ValueError('bad date')
            # flat table: index is zero-based month 0..11; leap-dependent bit only in
            # the February slot so it is read ONCE at the ONE comparison where a day
            # count depends on it, never during the per-day loop below.
            dpm = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)
            if m == 2:
                extra = 1 if ((y%4 == 0) and not (y%100 == 0)) or (y%400 == 0) else 0
                return dpm[m-1] + extra
            return dpm[m-1]
        # reject a day outside its own month (leap-year February is handled above)
        if not (1 &lt;= d1 &lt;= days_in_month(y1, m1)):
            raise ValueError('bad date')
        if not (1 &lt;= d2 &lt;= days_in_month(y2, m2)):
            raise ValueError('bad date')
        # all input valid; count through the first year to that day's start then each
        # full intervening year, then the last year. Every summand is a known integer,
        # so the whole result is int -- and it may be negative (for d1 later than d2).
&gt;       t = days_through(y1) + sum(dpm[0:m1-1]) + d1 - 1
                                   ^^^
E       NameError: name 'dpm' is not defined

solution.py:38: NameError</failure></testcase><testcase classname="artifacts.uplift.work_20260714-091030.days_between.tests.test_solution" name="test_common_cross" time="0.000"><failure message="NameError: name 'dpm' is not defined">&gt;   def test_common_cross(): assert f((2019, 2, 28), (2019, 3, 1)) == 1
                                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests\test_solution.py:7: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

d1 = 28, d2 = 1

    def days_between(d1, d2):
        if not (isinstance(d1, tuple) and isinstance(d2, tuple)):
            raise ValueError('bad date')
        def is_int(v):
            return isinstance(v, int) and type(v) is int
        if not all(is_int(a) for a in [d1[0], d1[1], d1[2]]):
            raise ValueError('bad date')
        if not all(is_int(a) for a in [d2[0], d2[1], d2[2]]):
            raise ValueError('bad date')
        y1, m1, d1 = d1
        y2, m2, d2 = d2
        # 365 days per year; leap-day correction (extra day in February of each
        # divisible-by-four year except centuries unless also a four-hundred-year
        # multiple) is handled at the level of total DAYS rather than months or years,
        # so there is no need to make any month-length table dependent on whether a
        # midpoint year across two calls was a leap year.
        def days_through(y):
            y -= 1
            return 365*y + (y//4) - (y//100) + (y//400)
        def days_in_month(y, m):
            if not (1 &lt;= m &lt;= 12): raise ValueError('bad date')
            # flat table: index is zero-based month 0..11; leap-dependent bit only in
            # the February slot so it is read ONCE at the ONE comparison where a day
            # count depends on it, never during the per-day loop below.
            dpm = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)
            if m == 2:
                extra = 1 if ((y%4 == 0) and not (y%100 == 0)) or (y%400 == 0) else 0
                return dpm[m-1] + extra
            return dpm[m-1]
        # reject a day outside its own month (leap-year February is handled above)
        if not (1 &lt;= d1 &lt;= days_in_month(y1, m1)):
            raise ValueError('bad date')
        if not (1 &lt;= d2 &lt;= days_in_month(y2, m2)):
            raise ValueError('bad date')
        # all input valid; count through the first year to that day's start then each
        # full intervening year, then the last year. Every summand is a known integer,
        # so the whole result is int -- and it may be negative (for d1 later than d2).
&gt;       t = days_through(y1) + sum(dpm[0:m1-1]) + d1 - 1
                                   ^^^
E       NameError: name 'dpm' is not defined

solution.py:38: NameError</failure></testcase><testcase classname="artifacts.uplift.work_20260714-091030.days_between.tests.test_solution" name="test_leap_year_span" time="0.000"><failure message="NameError: name 'dpm' is not defined">&gt;   def test_leap_year_span(): assert f((2000, 1, 1), (2001, 1, 1)) == 366
                                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests\test_solution.py:8: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

d1 = 1, d2 = 1

    def days_between(d1, d2):
        if not (isinstance(d1, tuple) and isinstance(d2, tuple)):
            raise ValueError('bad date')
        def is_int(v):
            return isinstance(v, int) and type(v) is int
        if not all(is_int(a) for a in [d1[0], d1[1], d1[2]]):
            raise ValueError('bad date')
        if not all(is_int(a) for a in [d2[0], d2[1], d2[2]]):
            raise ValueError('bad date')
        y1, m1, d1 = d1
        y2, m2, d2 = d2
        # 365 days per year; leap-day correction (extra day in February of each
        # divisible-by-four year except centuries unless also a four-hundred-year
        # multiple) is handled at the level of total DAYS rather than months or years,
        # so there is no need to make any month-length table dependent on whether a
        # midpoint year across two calls was a leap year.
        def days_through(y):
            y -= 1
            return 365*y + (y//4) - (y//100) + (y//400)
        def days_in_month(y, m):
            if not (1 &lt;= m &lt;= 12): raise ValueError('bad date')
            # flat table: index is zero-based month 0..11; leap-dependent bit only in
            # the February slot so it is read ONCE at the ONE comparison where a day
            # count depends on it, never during the per-day loop below.
            dpm = (31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)
            if m == 2:
                extra = 1 if ((y%4 == 0) and not (y%100 == 0)) or (y%400 == 0) else 0
                return dpm[m-1] + extra
            return dpm[m-1]
        # reject a day outside its own month (leap-year February is handled above)
        if not (1 &lt;= d1 &lt;= days_in_month(y1, m1)):
            raise ValueError('bad date')
        if not (1 &lt;= d2 &lt;= days_in_month(y2, m2)):
            raise ValueError('bad date')
        # all input valid; count through the first year to that day's start then each
        # full intervening year, then the last year. Every summand is a known integer,
        # so the whole result is int -- and it may be negative (for d1 later than d2).
&gt;       t = days_through(y1) + sum(dpm[0:m1-1]) + d1 - 1
                                   ^^^
E       NameError: name 'dpm' is not defined

solution.py:38: NameError</failure></testcase><testcase classname="artifacts.uplift.work_20260714-091030.days_between.tests.test_solution" name="test_bad_date" time="0.000" /><testcase classname="artifacts.uplift.work_20260714-091030.days_between.tests.test_solution" name="test_not_tuple" time="0.000" /><testcase classname="artifacts.uplift.work_20260714-091030.days_between.tests.test_solution" name="test_bool" time="0.000" /></testsuite></testsuites>