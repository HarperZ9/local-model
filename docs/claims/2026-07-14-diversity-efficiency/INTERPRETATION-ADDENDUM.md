# Interpretation addendum — frozen before the best-of-5 result exists

The preregistered band [0.21, 0.36] stands exactly as sealed. This addendum
does not touch it; it freezes what a hit or miss CAN and CANNOT mean, on
the record, while the deciding bench is still running — so no interpretation
is invented after the number arrives.

Adversarial review (independent math agent, arithmetic verified) found:

1. **A hit inside the band validates eta-constancy in N for the 3b only.**
   It does not confirm the cross-scale diversity claim; that claim needs the
   preregistered cross-model contrast in the E0-E6 protocol.
2. **A miss BELOW the band may falsify eta-constancy in N, not diversity.**
   Bimodal rescue distributions land at 0.19-0.22 with no change in
   diversity. A low miss sends the hypothesis to the per-task-corrected
   protocol, not to the grave — and equally, it earns no salvage narrative.
3. **The band's exact binomial coverage is ~0.909 at n=110** even if the
   point prediction is perfect; the adversary's noise-only restatement is
   [0.199, 0.367]. The sealed band governs the claim; the restatement is
   context, recorded here so it cannot be produced later as a rescue.
4. **The eta ordering (0.507 / 0.757 / 0.269) is currently uninterpretable
   as diversity evidence**: Jensen bias from task heterogeneity plus the
   greedy-first temperature schedule can produce it with zero diversity
   change, and eta(0.5b) rests on a single rescued task. Per-task pass-rate
   panels (protocol E1) are the mandatory discriminating measurement; the
   bench now records per-task outcome vectors for every future run.
5. **What already stands regardless**: the 3b uplift is real (12 rescues, 0
   regressions, exact paired test), and capability-per-GB shows a mid-scale
   OPTIMUM (peak at 3b: 0.064 wrapped-pass/B-param vs 0.036 at both ends),
   not a monotone smaller-is-better trend. The corrected claim is a
   sweet-spot claim.

Registered by the same hand that sealed the prediction, before its judge
returned.
